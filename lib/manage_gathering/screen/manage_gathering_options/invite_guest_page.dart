import 'package:flutter/material.dart';

class InviteGuestPage extends StatelessWidget {
  const InviteGuestPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.white,
    );
  }
}